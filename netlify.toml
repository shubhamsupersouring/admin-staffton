[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: send all routes to index.html (React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
