# Copy to .env.local for local dev. In Netlify: Site settings → Environment variables.
# Must be the full API base URL including /api/v1 (see src/services/apiClient.js).
VITE_API_BASE_URL=https://api-staffton.onrender.com/api/v1
